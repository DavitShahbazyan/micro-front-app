{
  "name": "micro-front-app",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.5",
  "workspaces": [
    "apps/*",
    "packages/*",
    "shared"
  ],
  "scripts": {
    "start": "concurrently -n \"SHARED,REMOTE,HOST\" -c \"green,cyan,magenta\" \"npm run start --workspace=shared\" \"npm run start --workspace=apps/remote\" \"npm run start --workspace=apps/host\"",
    "build": "turbo build",
    "build:fast": "turbo build --filter=[HEAD^] --include-dependencies",
    "build:shared": "turbo build --filter=@micro-front-app/shared",
    "build:host": "turbo build --filter=host",
    "build:remote": "turbo build --filter=remote",
    "build:legacy": "npm run build:shared && npm run build --workspaces --if-present",
    "serve": "concurrently -n \"SHARED,REMOTE,HOST\" -c \"green,cyan,magenta\" \"npm run serve --workspace=shared\" \"npm run serve --workspace=apps/remote\" \"npm run serve --workspace=apps/host\"",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "concurrently": "^8.2.2",
    "serve": "^14.2.5",
    "turbo": "^2.9.12"
  }
}
